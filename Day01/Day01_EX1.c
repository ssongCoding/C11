#include <stdio.h>

int main(void) {

    int score; // 변수를 선언 = 상자 소환!
    score = 7; // 변수를 초기화

    printf("%d", score);
    printf("score는 %d", score);
    printf("점수는 %d입니다.", score);

    return 0;
}